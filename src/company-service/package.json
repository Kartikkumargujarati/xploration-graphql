{
  "name": "company-service",
  "version": "1.0.0",
  "description": "Company federated service",
  "main": "index.ts",
  "scripts": {
    "build": "rimraf ./build && tsc",
    "start": "nodemon --exec ts-node index.ts"
  },
  "dependencies": {
    "@apollo/federation": "^0.23.0",
    "apollo-datasource-rest": "^0.12.0",
    "apollo-server": "^2.23.0"
  }
}